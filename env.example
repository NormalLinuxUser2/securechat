# SecureChat Environment Variables
# Copy this to .env and change the values

# Kill Switch Passcode (CHANGE THIS IMMEDIATELY!)
KILL_SWITCH_PASSCODE=SECURE_CHAT_KILL_SWITCH_2024

# Allowed Origins (comma-separated, or * for all)
ALLOWED_ORIGINS=*

# Port (Railway will set this automatically)
PORT=3000

# Security Settings
NODE_ENV=production
